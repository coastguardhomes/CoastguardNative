apply plugin: 'com.android.application'

android {
    namespace "com.coastguard"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.coastguard"
        minSdkVersion 23
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            shrinkResources false
            signingConfig signingConfigs.debug
        }
        debug {
            signingConfig signingConfigs.debug
        }
    }

    packagingOptions {
        pickFirst "**/*.so"
        pickFirst "**/*.c"
        pickFirst "**/*.h"
    }
}

dependencies {
    implementation project(':capacitor-android')
    implementation project(':capacitor-cordova-android-plugins')
}
