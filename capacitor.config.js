const config = {
  appId: "com.coastguard.app",
  appName: "CoastGuard",
  webDir: "dist",
  bundledWebRuntime: false,
  server: {
    androidScheme: "https",
    cleartext: true,
  },
};

export default config;
